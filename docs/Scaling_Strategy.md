# Multiple Data sources
In production level scenarios the data files are usually scattered and might be internally maintained by each cross functional teams and as TPM/ PM ensuring all the data sources are uptodate and provide them as input to agent to get the updates.
We can use a RAG framework for this usecase allowing relevant data retrieval for the user query on slack.

# Scheduled alerts

# Tradeoffs
